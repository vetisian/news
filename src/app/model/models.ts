export interface Apikey {
    apikey: string;
}

export interface Country {
    id: string;
    country: string;
    flag: string;
}

export interface News {
    id: string;
    title: string;
    link: string;
}
